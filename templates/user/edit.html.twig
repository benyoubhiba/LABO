
             					<div class="float-md-right dropdown-as-select" id="pageCountDatatable"> 
	 				
								 <div class="modal fade modal-right" id="edit" tabindex="-1" role="dialog"
                                aria-labelledby="edit" aria-hidden="true">
							
                                <div class="modal-dialog" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title">Add New</h5>
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                <span aria-hidden="true">&times;</span>
                                            </button>
                                        </div>
                                        <div class="modal-body">
	
                                             <input type="text" class="form-control" value="{{ user.cin }}" name="telephone">
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-outline-primary"
                                                data-dismiss="modal">Cancel</button>
                                            <button  type="submit" class="btnS btn-primary">Submit</button>
                                        </div>
                                   
                                </div>
							
                            </div>	 
												
					   </div>  
				

  <script>
        var dom = document.getElementsByClassName('form-control');
        function enabled() {
            document.getElementById('pageCountDatatable').getElementsByTagName('button')[0].removeAttribute('disabled');
        }
        for(let i=0;i<4;i++) {
            dom[i].addEventListener('keyup',enabled);
        }
    </script>

    <script>
     

        function envoye(event)
        {
            var btnS = document.getElementById('addEventListener').getElementsByClassName('btnS')[0];
           
            btn.setAttribute('hidden','hidden');
            btnspin.removeAttribute('hidden');

            var div = document.getElementsByName('myerror')[0];
            div.innerHTML='';
            div.removeAttribute('class');

            var dom = document.getElementById('addEventListener').getElementsByTagName('input');
            let data = JSON.stringify(
                {
                    "username" : dom[0].value,
                    "cin" : dom[1].value,
                    "email" : dom[2].value,
                    "phone" : dom[3].value,
                
                });
        

            
        }

        document.getElementById('addEventListener').querySelectorAll('a').forEach(function (link)
        {
            link.addEventListener('click',envoye);
        })
    </script>